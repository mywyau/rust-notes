use std::collections::HashMap;  // when we use the `use` keyword for Enums, Structs and other things we can specify the full path,


fn main() {
    let mut map = HashMap::new();
    map.insert(1, 2);
}
